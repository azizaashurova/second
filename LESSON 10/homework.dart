void main() {
   //--------------------- 1

  // List list = ["pdp", 4, "flutter best", 10.45, "online", 1, 2.7, 3, "android", 3.14];
  // List<String> stringList = [];
  // List<int> intList = [];
  // List<double> doubleList = [];

  // for (var element in list) {
  //   if (element is String) {
  //     stringList.add(element);
  //   } else if (element is int) {

  //     intList.add(element);
  //   } else if (element is double) {
  //     doubleList.add(element);
  //   }
  // }

  // for (var element in list) {
  //   if (element is String) {
  //     stringList.add(element);
  //   } else if (element is int) {
  //     intList.add(element);
  //   } else if (element is double) {
  //     doubleList.add(element);
  //   }
  // }

  // // Print the three lists
  // print("String list: $stringList");
  // print("Int list: $intList");
  // print("Double list: $doubleList");




//-----------------2

  // List<int> list = [1, 2, 3, 4, 5];
  // int largestElement = list.reduce((a, b) => a > b ? a : b);
  // print("The largest element in the array is $largestElement");



//-----------------3


  // List<int> list = [1, 2, 3, 4, 5];
  // int smallestElement = list.reduce((a, b) => a < b ? a : b);
  // print("The smallest element in the array is $smallestElement");





//-------------4
  // List<int> list = [1, 2, 3, 4, 5];
  // double average = list.reduce((a, b) => a + b) / list.length;
  // print("The average of the array elements is $average");



//-----------------5


  // int n = int.parse(stdin.readLineSync());
  // List<int> list = List.from(stdin.readLineSync().split(' '));
  // bool containsN = list.any((element) => element == n);
  // print(containsN ? "true" : "false");






}









